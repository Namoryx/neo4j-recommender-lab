const API_BASE = 'https://neo4j-runner.neo4j-namoryx.workers.dev';

const RUN_URL = `${API_BASE}/run`;
const SUBMIT_URL = `${API_BASE}/submit`;
const SEED_URL = `${API_BASE}/seed`;
const HEALTH_URL = `${API_BASE}/health`;

export { API_BASE, RUN_URL, SUBMIT_URL, SEED_URL, HEALTH_URL };
